# Copy to .streamlit/secrets.toml; never commit that file.
GEMINI_API_KEY = "PASTE_YOUR_KEY_LOCALLY"
GEMINI_MODEL = "gemini-3.6-flash"
DAILY_REQUEST_LIMIT = "10"
# Required before exposing the application on the internet.
APP_PASSWORD = "CHANGE_THIS_PASSWORD"
